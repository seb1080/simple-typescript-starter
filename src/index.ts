// eslint-disable-next-line no-console

const OUT_PUT = `Hello world!`;
const secondVariable = `Web Dev. env. is all good :)`;

const array1 = ['value1', 'value2'];

console.log(`${OUT_PUT}, ${secondVariable}`);
